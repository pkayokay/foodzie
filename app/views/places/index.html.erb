  <% @places.each do |place| %>
    <div class="food-box">
      <!--<i><%= place.address %></i>
        <% if place.defaultpicture.present? %> 
        <br><br><%= image_tag place.defaultpicture, class: 'img' %>
        <% end %>
      <p><%= place.description %></p>
      <% if place.user.present? %>
        <small style="text-align: left">Added by: <%= place.user.email %></small>
      <% end %>
      <br>
      <br>-->
      <%= link_to place_path(place) do %>        
        <div class="image-box">
          <div class="caption">
            <h1><%= place.name %> </h1>
            <p><%= place.description %></p>
            <p><i><%= place.address %></i></p>
          </div>
          <%= image_tag place.defaultpicture, class: 'img' %> <!--remove this and add above -->        
        </div>
      <% end %>
    </div>
  <% end %>
  <br>
<div class="center"> 
  <%= will_paginate @posts %>
</div>



